global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'ssv-liquidator'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['ssv-liquidator:${PORT}']
        labels:
          app: 'liquidation-cli'
