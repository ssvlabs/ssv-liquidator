# Prod SHIFU
---
apiVersion: v1
kind: Service
metadata:
  name: ssv-liquidator-worker-7-svc
  namespace: {{ .Values.namespace }}
  labels:
    name: ssv-liquidator-worker-7-svc
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: 3000
  selector:
    app: ssv-liquidator-worker-7
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ssv-liquidator-worker-7
  name: ssv-liquidator-worker-7
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: ssv-liquidator-worker-7
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ssv-liquidator-worker-7
    spec:
      containers:
      - name: ssv-liquidator-worker-7
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        command: ["/bin/sh"]
        args: ["-c", "yarn cli"]
        env:
        - name: NODE_ENV
          valueFrom:
            secretKeyRef:
              name: config-secrets
              key: node_env

        - name: LOG_LEVEL
          value: "{{ .Values.app.logLevel }}"

        - name: NO_COLOR
          value: "{{ .Values.app.noColor }}"

        - name: NODE_URL
          valueFrom:
            secretKeyRef:
              name: config-secrets
              key: node_url_mainnet

        - name: GAS_PRICE
          value: "high"

        - name: HIDE_TABLE
          value: "1"

        - name: ACCOUNT_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: config-secrets
              key: account_private_key

        - name: SSV_SYNC_ENV
          valueFrom:
            secretKeyRef:
              name: config-secrets
              key: ssv_sync_env_7

        - name: SSV_SYNC
          valueFrom:
            secretKeyRef:
              name: config-secrets
              key: ssv_sync_7

#        volumeMounts:
#        - mountPath: "/data/"
#          name: ssv-liquidator-worker-7
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
#      volumes:
#      - persistentVolumeClaim:
#          claimName: ssv-liquidator-worker-7
#        name: ssv-liquidator-worker-7
        {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
